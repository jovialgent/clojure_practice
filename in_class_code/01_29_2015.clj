; Hash Maps in Clojure
; (hash-map :key :val :key1 :val1)
; { :key :val :key1 :val1}
; (keys) ---> List of keys
; (vals -map-) ---> List of values
; (assoc -map- :key :value :key :value) ---> creates a new map (not update) with new key and values 
; 

; Condp 
; (cond
;	(> temp 80) "hot"
; 	)
;
;
;  lein 

; Binding
;
; Do things differently from let 
; Dynamic Variables